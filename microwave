#!/bin/bash
# your code goes here
git clone https://$GITHUB_TOKEN@github.com/vcyzteen/Neko-CL.git
cd Neko-CL
mkdir .github/workflows
mv .github/action.yml .github/workflows/action.yml
git add .
git commit -m "NekoCL: missing import github action" --signoff 
git branch -M neko
git push https://$GITHUB_TOKEN@github.com/vcyzteen/Neko-CL.git neko
