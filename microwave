#!/bin/bash
# your code goes here
# git clone https://github.com/vcyzteen/linux.git -b 4.4-rev
# cd linux
# git cherry-pick 85e4fcfe0f5bf2285e78aa22efb54bda0209732a 85e4fcfe0f5bf2285e78aa22efb54bda0209732a d9c36fd359b6f957293f7e5fc6b49bd67012d919 0d66d4b3b3fb003cffb6d78f212ddc709be911e8 404194e40d242b85d05415f954ae90fee1392af3 81c6cf71fa87aedaca8bd2ac433def4da94a9a85
# git push https://$GITHUB_TOKEN@github.com/vcyzteen/linux.git
# wget https://raw.githubusercontent.com/vcyzteen/myscripts/edge/kernel.sh
# bash kernel.sh
# git clone https://$GITHUB_TOKEN@github.com/vcyzteen/gcc-build.git
# cd $HOME
# git clone https://github.com/vcyzteen/gcc-arm64-4.9.git arm -b neko-v1
# git clone https://github.com/vcyzteen/gcc-arm64-4.9.git edge -b edge
# cp -r edge/* arm/
# cd arm
# git clone https://$GITHUB_TOKEN@github.com/vcyzteen/gcc-build.git
# mv gcc-build/build-gcc.sh $HOME
# rm -rf gcc-build
# chmod +x build-gcc.sh
# ./build-gcc.sh -a arm
# cd ../gcc-arm
# ./bin/arm-linux-gnueabi-gcc -v 2>&1 | tee /tmp/gcc-arm-version
# ./bin/arm-linux-gnueabi-ld.bfd -v 2>&1 | tee /tmp/bfd-arm-version
# git init
# git add .
# git commit -as -m "Import GCC : Neko@Vcyz" -m "Configuration: $(/bin/cat /tmp/gcc-arm-version)" -m "BFD: $(/bin/cat /tmp/bfd-arm-version)"
# git gc
# git commit -as -m "Import GCC : Neko@Vcyz" 
# git push https://$GITHUB_TOKEN@github.com/vcyzteen/gcc-arm64-4.9.git neko-v1
git clone https://github.com/vcyzteen/vcyzteen.git
cd vcyzteen
rm -rf *.png
git add . && git commit -m "delete all img" --signoff
git push https://$GITHUB_TOKEN@github.com/vcyzteen/vcyzteen.git
