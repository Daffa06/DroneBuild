#!/bin/sh
# your code goes here
neofetch
git clone https://github.com/xanmod/linux -b 5.16 xanmod
git clone https://github.com/vcyzteen/linux.git -b 4.4-cfs
cp -r xanmod/drivers/android linux/drivers
cd linux
git add . && git commit -m "upstream: binder: import latest binder from tag 5.16" --signoff
git push https://$GITHUB_TOKEN@github.com/vcyzteen/linux.git 4.4-cfs
# wget https://raw.githubusercontent.com/vcyzteen/myscripts/min/kernel.sh
# bash kernel.sh
