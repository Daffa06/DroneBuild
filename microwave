#!/bin/bash
# your code goes here
git clone https://github.com/vcyzteen/Neko-CL.git
cd Neko-CL
mkdir .github
echo > .github/action.yaml
git add .
git commit -m "NekoCL: initial import github action" --signoff 
git branch -M neko
git push https://$GITHUB_TOKEN@github.com/vcyzteen/Neko-CL.git neko
