#!/bin/bash
# your code goes here
# git clone https://github.com/vcyzteen/linux.git -b 4.4-rev
git clone https://github.com/vcyzteen/linux.git -b 4.4-new
# git clone https://github.com/xanmod/linux.git xanmod -b 5.15
# cp -r xanmod/net/ipv4/tcp* linux/net/ipv4
# cp -r xanmod/net/ipv6 linux/net
cd linux
git fetch https://github.com/arter97/exfat-linux.git master
git merge -s ours --no-commit FETCH_HEAD --allow-unrelated-histories
git read-tree --prefix=fs/exfat -u FETCH_HEAD
git commit -m "Merged exfat https://github.com/arter97/exfat-linux into MinerV" --signoff
# git reset --hard 3faba7ea06739257aa6bca150c2f3952366b9989
# wget https://dlcdnets.asus.com/pub/ASUS/ZenFone/ZB601KL/ZB601KL-17.2017.2008.432-kernel.tar.gz
# tar -xvf ZB601KL-17.2017.2008.432-kernel.tar.gz
# cd msm-4.4
# cp -r msm-4.4/include/linux linux/include
# cd linux
# rm -rf .git
# git init
# git branch -M master
# git add . && git commit -m "[TCP] test net from latest kernel" --signoff
# git push https://$GITHUB_TOKEN@github.com/vcyzteen/perf-original.git master -f
# git push https://$GITHUB_TOKEN@github.com/vcyzteen/linux.git 4.4-rev
# git log 10
# git diff
# git cherry-pick 85e4fcfe0f5bf2285e78aa22efb54bda0209732a 85e4fcfe0f5bf2285e78aa22efb54bda0209732a d9c36fd359b6f957293f7e5fc6b49bd67012d919 0d66d4b3b3fb003cffb6d78f212ddc709be911e8 404194e40d242b85d05415f954ae90fee1392af3 81c6cf71fa87aedaca8bd2ac433def4da94a9a85
# git push https://$GITHUB_TOKEN@github.com/vcyzteen/linux.git
# wget https://raw.githubusercontent.com/vcyzteen/myscripts/testgcc/kernel.sh
# bash kernel.sh
# wget https://raw.githubusercontent.com/vcyzteen/gcc-build/main/build-gcc.sh
# git clone https://github.com/vcyzteen/gcc-arm64.git ../gcc-arm64 -b neko
# rm -rf ../gcc-arm64/*
# chmod a+x build-*.sh
# ./build-gcc.sh -a arm64
# cd ../gcc-arm64
# ./bin/aarch64-linux-gnu-gcc -v 2>&1 | tee /tmp/gcc-arm64-version
# git add . -f
# git commit -as -m "Build NeVixTC: Arm64" -m "Build completed on: $(/bin/date)" -m "Configuration: $(/bin/cat /tmp/gcc-arm64-version)" --signoff# 
# git gc
# git push https://$GITHUB_TOKEN@github.com/vcyzteen/gcc-arm64.git neko -f
git push https://$GITHUB_TOKEN@github.com/vcyzteen/linux.git 4.4-new
