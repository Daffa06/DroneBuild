#!/bin/bash
# your code goes here
git clone https://github.com/Panchajanya1999/tc-build
cd tc-build
git fetch https://github.com/ClangBuiltLinux/tc-build
git merge FETCH_HEAD
git status
git add .
git commit -m "NekoCL: initial import" --signoff 
git branch -M neko
git push https://$GITHUB_TOKEN@github.com/vcyzteen/NekoCL.git neko
